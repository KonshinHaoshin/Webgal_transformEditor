# 如何使用 WebGAL Transform Editor

本文档以教程形式介绍 **WebGAL Transform Editor**（WebGAL 运镜脚本编辑器）的安装、界面与日常用法。  
采用分节结构，便于按需查阅。适合剧本作者、演出设计者以及需要编辑 WebGAL 立绘变换与脚本的用户。

---

## 1. 简介

### 软件

![](https://pic1.imgdb.cn/item/699d2817fbf7002998e126c6/e040e6ca8a13ae85984e68c3647f4ea67b3b93ff.webp)

### 是什么？

**WebGAL Transform Editor** 是一款桌面应用，用于**可视化编辑 WebGAL 的立绘位置、缩放与旋转**，并**导出**对应的 `setTransform`、`changeFigure` 等脚本指令。

- **核心用途**：在画布上拖拽、缩放、旋转立绘，设置滤镜,进行截图,自动生成或更新 WebGAL 脚本等，无需手写坐标与参数。
- **技术栈**：基于 [Tauri](https://tauri.app) + [React](https://react.dev) + [TypeScript](https://www.typescriptlang.org/) + [PixiJS](https://pixijs.com/)，支持 Live2D（Cubism 2）与jsonl,webgal_mano,图片、视频等资源。

### 主要能力概览

- **变换编辑**：位置（x, y）、缩放（scaleX, scaleY）、旋转（弧度），支持多选与批量操作。
- **脚本解析与导出**：粘贴或加载 WebGAL 脚本后，在画布上编辑，变更会实时反映到脚本；支持导出 `setTransform`、`changeFigure` 及动画参数（duration、ease）。
- **WebGAL 模式**：连接游戏根目录后，从 `figure` / `background` 中直接选择立绘与背景，支持 `.json` / `.jsonl` Live2D 模型。
- **辅助工具**：观测层（颜色/明度）、辅助线（三分法、黄金比例等）、滤镜预设与自定义滤镜、动画预览与断点调试。

---

## 3. 界面与基本操作

### 整体布局

- **画布（中央）**：显示当前立绘、背景及辅助线；在此进行拖拽、缩放、旋转。
- **工具栏（顶部）**：画幅比、辅助线、观测层、滤镜、全选/取消全选等。
- **左侧 / 右侧面板**：脚本输入、资源选择（WebGAL 模式下）、属性与滤镜参数等。
- **输出脚本窗口**：可独立打开，显示解析后的脚本行，支持编辑、复制、断点与实时同步。

![](https://pic1.imgdb.cn/item/699d26e3fbf7002998e12664/c451b4c21d29159e88f7eeb08158070b83da8b99.webp)

### 画布上的基本操作


| 操作       | 方法                                                        |
| -------- | --------------------------------------------------------- |
| **移动**   | 点击模型并拖拽。                                                  |
| **多选移动** | 按住 `Shift` 依次点击多个模型，或点击「Select All」全选，再拖拽其中任意一个，选中项会一起移动。 |
| **缩放**   | 按住 `Ctrl` 并滚动鼠标滚轮：向上放大，向下缩小；缩放以鼠标位置为参考。                   |
| **旋转**   | 按住 `Alt` 并拖拽模型；绕模型中心旋转，角度以弧度显示。                           |
| **取消选择** | 点击「Deselect All」或点击画布空白处。                                 |


### 选择与多选

- **单选**：直接点击一个模型。
- **多选**：按住 `Shift` 再点击多个模型。
- **全选 / 取消全选**：使用工具栏上的「Select All」「Deselect All」按钮。

---

## 4. WebGAL 模式：连接游戏与加载资源

### 启用 WebGAL 模式

1. 勾选界面上的 **「WebGAL模式」** 复选框。
2. 在弹出对话框中选择你的 **WebGAL 游戏根目录**（即包含 `game` 等文件夹的目录）。
3. 选择后，编辑器会识别 `game/figure`（立绘）与 `game/background`（背景）目录。

### 加载立绘与背景

- **立绘**：在 WebGAL 模式下从列表或选择器中选择 `figure` 目录中的文件。  
  - 支持：图片（`.png`、`.jpg` 等）、Live2D 单体模型（`.json`）、**JSONL 聚合模型**（`.jsonl`）。  
  - 仅支持 **Cubism 2** 类型 Live2D（如常见「你邦」模型）。
- **背景**：选择背景时，从 `background` 目录中选取；选中的背景会显示在画布上。

### 关于 JSONL 聚合模型

若立绘为 `.jsonl` 文件（多部件组合立绘），编辑器会按聚合模型加载，画布上显示为一个整体，可整体移动、缩放、旋转。格式与用法详见jsonl条目

![](https://pic1.imgdb.cn/item/699d2726fbf7002998e1267b/b2a8705b82003e0d99e905684de132421aed3044.webp)

---

## 5. 脚本的加载、编辑与导出

### 加载与解析脚本

1. 在**左侧脚本输入框**中粘贴或输入 WebGAL 脚本（含 `setTransform`、`changeFigure` 等）。
2. 点击 **「Load Script」**，会打开**输出脚本**窗口。
3. 脚本被解析后，画布上会显示对应的立绘、背景及变换状态。

![](https://pic1.imgdb.cn/item/699d278dfbf7002998e1269c/95edc5520c025701ab6a7dd43c16f10621f7b47a.webp)

### 脚本窗口中编辑

- **实时同步**：在输出脚本窗口中修改某行内容后，主窗口画布会实时更新。
- **添加/移除** `-next`：点击每行左侧的 **「N」** 按钮。
- **断点**：点击每行左侧的 **「○」** 可设置断点，动画播放时会在该处暂停。
- **删除行**：点击该行右侧的 **「×」**。
- **换行**：`Enter` 提交当前行；`Shift + Enter` 在行内插入换行。

### 复制与导出

- **复制全部脚本**：在输出脚本窗口点击 **「复制脚本」**。
- **只复制 setTransform**：点击 **「只复制setTransform语句」**，便于只把变换指令贴回剧本。

导出行为说明：

- 在画布上进行的移动、缩放、旋转会**实时**写入输出脚本中的 `setTransform` 等指令。
- 可设置动画的 **duration**（持续时间）和 **ease**（缓动函数）。
- **增量合并**：`setTransform` 会继承上一句未显式指定的属性，只写出有变化的部分。

### 支持的脚本格式示例

**setTransform：**

```webgal
setTransform figure1 -duration=500 -x=100 -y=200 -scaleX=1.5 -scaleY=1.5 -rotation=0.5;
```

**changeFigure：**

```webgal
changeFigure figure1 -motion=idle -expression=happy -id=character1 -duration=0;
```

常用参数：`position`（x, y）、`scale`（scaleX, scaleY）、`rotation`（弧度）、`duration`、`ease`、`-next` 等。

---

## 6. 辅助功能

### 观测层（颜色 / 明度）

- 在顶部工具栏找到 **「观察层」** 下拉菜单，选择 **「颜色」** 或 **「明度」**。
- **颜色** 模式：用于观察画面**明度**分布（中性灰 + 颜色混合）。
- **明度** 模式：用于观察**色彩**分布（中性灰 + 明度混合）。
- ⚠️ **注意**：开启观测层时画布**不可交互**（不能拖拽、缩放、旋转），便于专注查看构图与层次。关闭时选择 **「无」**。

### 辅助线

- 在 **「辅助线」** 下拉菜单中选择类型：
  - **无辅助线**：关闭。
  - **三分法**：三分构图线。
  - **中心十字**：中心十字线。
  - **对角线**：对角线。
  - **黄金比例**：黄金比例线。
- 辅助线仅叠加显示，不影响拖拽与编辑，可用于确认构图后再定稿。

### 滤镜

- **预设**：勾选「启用滤镜预设」，在预设列表中选择后，点击 **「应用到选中模型」** 或 **「应用到所有模型」**。
- **自定义**：在右侧滤镜面板中调整参数，效果会实时反映到画布。
- **保存预设**：可将当前参数保存为自定义预设（存储在浏览器本地），方便复用。
- **应用范围**：仅选中模型 / 全部模型（含背景），按需选择。

### 画幅比

- 在工具栏选择画幅比：**16:9**、**21:9**、**1.85:1**、**16:10**、**4:3** 或 **自定义**。
- 画布宽度随画幅比变化，高度固定为 1440px（与 WebGAL 常见分辨率 2560×1440 对应）。  
若需更改 WebGAL 项目分辨率，请参考项目或作者提供的教学说明。

### 动画预览与断点

- 点击 **「播放」** 可预览当前脚本的动画效果。
- 在输出脚本窗口中为某行设置**断点**后，播放会在该行暂停，便于检查关键帧。

---

## 7. 常见问题（FAQ）

**Q：如何只改位置不改缩放/旋转？**  
A：在画布上只做拖拽即可；导出的 `setTransform` 会沿用上句的缩放、旋转（增量合并）。若需单独改某一项，可在输出脚本中手动编辑该行参数。

**Q：Live2D 不显示或报错？**  
A：请确认为 **Cubism 2** 格式；Cubism 3/4 等暂不支持。若为 `.jsonl` 聚合模型，请确保每行 JSON 与子模型路径正确，参见 [JSONL 聚合模型说明](JSONL聚合模型说明.md)。

**Q：观测层下无法拖拽模型？**  
A：这是设计如此。观测层用于「只看不改」，请将观察层选回「无」后再进行拖拽、缩放、旋转。

**Q：如何把编辑结果用到 WebGAL 里？**  
A：在输出脚本窗口使用「复制脚本」或「只复制setTransform语句」，将内容粘贴到你的 WebGAL 剧本文件中即可。

**Q：滤镜会写入脚本吗？**  
A：本编辑器主要导出 `setTransform`、`changeFigure` 等变换与立绘指令；滤镜为编辑器内预览与效果设计用，是否写入游戏需视 WebGAL 与你的工作流而定。

**Q：支持哪些系统？**  
A：Windows、macOS、Linux。需满足上述 Node.js、Rust 及系统依赖要求。

---

## 8. 相关链接与文档

- **项目仓库**：[Webgal_transformEditor](https://github.com/KonshinHaoshin/Webgal_transformEditor)  
- **项目说明**：[README](README.md) — 功能列表、构建与打包、技术栈。  
- **JSONL 聚合模型**：[JSONL聚合模型说明](JSONL聚合模型说明.md) — `.jsonl` 格式与在编辑器中的用法。  
- **WebGAL**：支持在剧本中使用 `setTransform`、`changeFigure` 等指令控制立绘与变换。

---

*本文档按 Grokpedia 风格编写，便于按节查阅与入门使用。*